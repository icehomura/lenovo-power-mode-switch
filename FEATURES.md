# 功能说明

Lenovo Power Mode Switch 是一款基于 Tauri v2 的轻量级 Lenovo 电源模式切换工具，通过逆向工程的 DLL 桥接层直接与硬件通信，无需依赖 Lenovo Vantage 或联想电脑管家。仅支持 Windows x86-64。

## 核心功能

### 电源模式切换

支持四种电源模式，每种模式由硬件能力掩码动态检测可用性：

| 模式 | 标识 | 说明 | 图标 |
|------|------|------|------|
| 智能模式 | `intelligent` | 按负载自动调节性能与功耗 | ⚖️ |
| 省电模式 | `saving` | 风扇更安静、功耗更低 | 🍃 |
| 性能模式 | `performance` | 释放持续性能 | ⚡ |
| 极客模式 | `geek` | 最高性能，需硬件支持 | 🚀 |

极客模式有三种状态：`unsupported`（硬件不支持）、`available`（可用）、`greyed`（硬件支持但被系统置灰，如电池供电时）。

### 系统托盘

- 左键点击托盘图标：显示/隐藏主窗口
- 右键菜单：切换四种电源模式（当前模式带勾选态）、显示/隐藏悬浮窗、显示主窗口、退出
- 托盘提示实时显示当前电源模式
- 勾选态与提示文字在任何来源的模式变更后都会同步（托盘自身、全局快捷键、悬浮窗点击、硬件轮询）

### 悬浮窗

悬浮窗在运行时按需创建（`WebviewWindowBuilder`），而非在 `tauri.conf.json` 中静态声明——只在用户真正启用时才存在，因此「启用」就是一次真实的新建，而非空写设置。

- 透明无边框、始终置顶的小窗，首次启用时创建
- 两种样式（设置页可选）：
  - **胶囊型**：窄条，仅显示当前模式图标（44×28）
  - **圆形**：显示模式图标与模式名（96×96）
- **左键单击**：循环切换到下一个硬件支持的模式
- **右键单击**：在两种样式间切换
- **拖拽移动**：按 DPI 缩放换算指针位移，避免高缩放屏下窗口漂移
- 可吸附到屏幕四角（设置页可选）
- 关闭请求（Alt+F4）只隐藏窗口，不销毁

样式、位置与启用状态持久化在 `settings.json`，启动时由后端自动恢复。

### 全局快捷键

- 为每种电源模式绑定全局快捷键（如 `Ctrl+Alt+1` 到 `Ctrl+Alt+4`）
- 额外的「循环切换模式」绑定：按一次切到下一个硬件支持的模式
- 所有绑定默认留空，由用户在设置页自行录制
- 快捷键冲突检测：同一快捷键不能绑定到不同模式
- 按下快捷键后的切换由 **Rust 侧直接执行**，因此主窗口隐藏时同样有效
- 按键名大小写不敏感（`F5`、`Space`、`Up` 等均可绑定）
- 绑定通过 `tauri-plugin-store` 持久化，重启后自动恢复注册

### 开机自启

- 通过 `tauri-plugin-autostart` 实现开机自动启动
- 可在设置中启用/禁用

### 窗口状态持久化

- 通过 `tauri-plugin-window-state` 自动保存窗口位置和大小
- 重启应用后恢复上次窗口状态

## 技术架构

### DLL 桥接层

应用通过 `libloading` 动态加载 `lpm_bridge.dll`，这是一个独立的 C++/CLI 混合模式 DLL，驱动 Lenovo IdeaNotebookAddin / PowerBattery 原生层的隔离副本。

关键特性：
- 不触碰已安装的 Lenovo Vantage 服务
- 不需要 RPC 或签名信任握手
- DLL 文件放置在可执行文件同级目录，确保使用隔离副本

桥接层提供以下 C ABI 接口：

| 函数 | 说明 |
|------|------|
| `lpm_init` | 初始化桥接层 |
| `lpm_set_mode(i32)` | 设置电源模式（1-4） |
| `lpm_get_mode()` | 读取当前电源模式 |
| `lpm_capability()` | 读取硬件能力掩码 |
| `lpm_supported_modes()` | 读取支持的模式掩码 |
| `lpm_geek_state()` | 读取极客模式状态 |
| `lpm_last_error_copy()` | 复制最近错误信息 |

### 状态同步机制

后端是硬件状态的**唯一真相源**：

- 后端线程每 3 秒读取一次硬件模式，一旦变化就向所有窗口广播 `mode-changed`
- 所有切换路径（托盘、快捷键、悬浮窗点击、主窗口按钮）都经由后端命令，切换成功后走同一条广播
- 各窗口只监听 `mode-changed`，不再各自轮询，消除了原先三处重复轮询
- 外部程序（如 Lenovo Vantage）造成的变更由后端轮询捕获后同样广播
- 通过 `tauri-plugin-single-instance` 保证单实例，避免多个实例争抢同一硬件
- 窗口获得焦点时额外做一次全量刷新，重新检测模式可用性（如极客模式在电池供电时可能被置灰）

### 模式切换验证

切换模式后，后端会回读硬件实际状态并对比，只有硬件确认接受切换后才返回成功，避免 UI 显示与实际不一致。

## 插件依赖

| 插件 | 用途 |
|------|------|
| `tauri-plugin-global-shortcut` | 全局快捷键注册与管理 |
| `tauri-plugin-autostart` | 开机自启 |
| `tauri-plugin-single-instance` | 单实例保护，避免多实例争抢硬件 |
| `tauri-plugin-store` | 持久化存储（快捷键绑定、悬浮窗设置等） |
| `tauri-plugin-window-state` | 窗口状态持久化 |
| `tauri-plugin-shell` | 系统 shell 操作 |
