# 更新说明

## v1.1.0（当前版本）

### 新增功能

- **悬浮窗运行时新建**：悬浮窗改为按需创建（`WebviewWindowBuilder`），启用即新建，不再依赖静态声明
- **悬浮窗样式**：胶囊型仅显示模式图标（44×28），圆形显示图标与名称（96×96），设置页可切换
- **悬浮窗交互**：左键单击循环切换模式，右键切换样式
- **循环切换快捷键**：新增独立的「循环切换模式」全局快捷键绑定，默认留空
- **快捷键下沉到后端**：按下快捷键后由 Rust 直接切换硬件，主窗口隐藏时同样生效
- **托盘勾选态**：托盘菜单中当前模式带勾选，并新增「显示/隐藏悬浮窗」项
- **单实例保护**：接入 `tauri-plugin-single-instance`，避免多实例争抢硬件
- **启动恢复悬浮窗**：样式、位置与启用状态在启动时由后端自动恢复

### 修复

- **按键名大小写**：`parse_shortcut` 原先只匹配小写分支，导致 `Shift+F5`、`Ctrl+Space`、`Ctrl+Up` 等绑定报「未知按键」；现改为大小写不敏感
- **Super 修饰键丢失**：`Shortcut::new` 在 Windows 上把 `META` 规范化为 `SUPER`，而 `format_modifiers` 只检查 `META`，会把 Super 修饰键静默丢弃；现两种拼写都接受
- **悬浮窗拖拽漂移**：高 DPI 屏下混用物理像素与逻辑像素导致窗口跟不上指针；现按 `devicePixelRatio` 换算
- **移除重复轮询**：主窗口与悬浮窗原先各自每 3 秒轮询硬件，现统一由后端广播，消除三处重复
- **清理死代码**：移除失效的 `power-mode-changed` 事件通道与未注册的命令

### 测试

- `cargo test`：83 项通过
- `vitest`：63 项通过
- `vue-tsc --noEmit`：无类型错误

## v1.0.0

### 新增功能

- **电源模式切换**：支持智能、省电、性能、极客四种模式，基于硬件能力掩码动态检测可用性
- **系统托盘**：托盘图标左键切换窗口显示/隐藏，右键菜单直接切换模式
- **悬浮窗**：透明无边框置顶小窗，适合快速模式查看与切换
- **全局快捷键**：为每种电源模式绑定全局快捷键，支持自定义组合键
- **开机自启**：通过系统托盘设置开机自动启动
- **窗口状态持久化**：自动保存和恢复窗口位置、大小
- **快捷键持久化**：快捷键绑定存储在本地，重启后自动恢复

### 技术实现

- 基于 Tauri v2 + Vue 3 + TypeScript 技术栈
- 通过 `libloading` 动态加载逆向工程的 `lpm_bridge.dll` 桥接层
- 桥接层为 C++/CLI 混合模式 DLL，驱动 Lenovo 硬件电源管理接口
- 独立于 Lenovo Vantage 服务运行，无需 RPC 或签名信任

### 权限配置

- 配置了完整的 Tauri v2 capabilities，覆盖窗口管理、事件系统、快捷键、存储、自启等权限
- 安全策略（CSP）限制为自身资源加载，禁止外部脚本

### 已知限制

- 仅支持 Windows 平台（依赖 Lenovo 硬件 DLL）
- 极客模式在电池供电时可能被系统置灰
- 需要 `lpm_bridge.dll` 及其依赖的 addin DLL 文件位于可执行文件同级目录
